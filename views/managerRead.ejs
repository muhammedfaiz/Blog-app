<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Blog View - Admin Panel</title>
  <!-- Include Bootstrap CSS -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
  <div class="container py-4">
    <div class="row">
      <div class="col-md-8 mx-auto">
        <div class="card">
          <div class="card-header">
            <h3 class="card-title"><%= blog.title%></h3>
            <span class="card-date">Posted by <%= blog.userName%> on <%= blog.date%></span><br>
            
          </div>
          <div class="card-body">
            <p class="card-text"> Description: <%= blog.description%></p>
            <hr>
            <h4>Content:</h4>
            <p>
              <%= blog.content%>
            </p>
          </div>
          <div class="card-footer d-flex justify-content-between">
            <form action="" method="post">
            <button type="submit" name="approved" class="btn btn-success" value=1>Approve</button>
            <button type="submit" name="approved" class="btn btn-danger" value=-1>Decline</button>
        </form>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Include Bootstrap JS and Popper.js -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
